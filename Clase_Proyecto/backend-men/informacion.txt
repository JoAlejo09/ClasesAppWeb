//NOS PERMITE GENERAR UNA SIMPLIFICACION DE COMANDOS MEDIANTE CODIGOS
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  SI SE EJECUTA nmp start - se ejecuta lo que esta en el comando
  
  /*DIRECTORIOS EXPRESOS*/
  .env --> Variables de entorno ocultas que no se muestran ni se implementan
  .env.example --> Variables publicas accesibles por el usuario.

  